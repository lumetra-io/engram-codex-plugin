---
description: Search Engram memory using natural language
argument-hint: <what to look up>
---

Use the `query_memory` tool from the Engram MCP server to search memory for:

$ARGUMENTS

Surface the most relevant memories with their explanation traces (why each was matched). If nothing relevant comes back, say so explicitly rather than fabricating context.
