---
description: Store a fact or piece of context in Engram memory
argument-hint: <what to remember>
---

Use the `store_memory` tool from the Engram MCP server to save the following content to memory:

$ARGUMENTS

After storing, briefly confirm what was saved and which bucket it went to. Keep the fact atomic and declarative — one concept per memory works best. If the input is long or covers multiple facts, split it into separate `store_memory` calls.
